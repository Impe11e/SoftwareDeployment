- name: install nginx
  apt:
    name: nginx
    state: present

- name: deploy nginx config
  template:
    src: nginx.conf.j2
    dest: /etc/nginx/sites-available/default
  notify: restart nginx

- name: start nginx
  service:
    name: nginx
    state: started
    enabled: yes
